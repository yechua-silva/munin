[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    gpu: marks tests that require a real GPU (ROCm/CUDA)
    slow: marks tests as slow (deselect with -m "not slow")
